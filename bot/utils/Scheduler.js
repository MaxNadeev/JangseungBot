/**
 * Планировщик задач на основе setInterval/cron.
 * @method addTask(name, callback, interval) - Добавить задачу.
 * @method stopTask(name) - Остановить задачу.
 */
class Scheduler {
    constructor() {
      this.tasks = new Map();
    }
  }